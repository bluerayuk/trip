/* ============ Sample packages ============ */
// Shared-price packages used by the sample itinerary. Each entry's `id` is
// referenced by `packageId` on stops in SAMPLE_PLACES (see sample-places.js),
// so if you rename or remove an id here, update those stops too.
//
// Plain (non-module) script: load this BEFORE app.js in index.html.

const SAMPLE_PACKAGES = [
  {
    "id": "pkg-1788129485954n4a8xunrpce",
    "name": "Statue City Cruises",
    "cost": 26
  }
];
